# DNS Caching Strategy

This document defines the caching strategy for PowerBlockade, including baseline cache settings, image versions, precache policies, domain selection criteria, and primary/secondary synchronization.

---

## Current Baseline

This section documents the exact current runtime settings and image references extracted from repository-tracked configuration files.

### Image/Version References

| Component | Source of Truth (compose.yaml) | GHCR Deployment | Notes |
|-----------|-------------------------------|-----------------|-------|
| dnsdist | `powerdns/dnsdist-20:2.0.8` (line 29) | `powerdns/dnsdist-20:2.0.8` (line 21) | Official PowerDNS image, version 20 series |
| recursor | Built from `./recursor` Dockerfile | `ghcr.io/${POWERBLOCKADE_REPO:-zerostate-io}/powerblockade-recursor:${POWERBLOCKADE_VERSION:-latest}` (line 72) | Custom image based on upstream |
| recursor base | `powerdns/pdns-recursor-53:5.3.10` (Dockerfile:1) | Same | Version 53 series |
| recursor-reloader | Built from `./recursor-reloader` (compose.yaml:243) | `powerdns/pdns-recursor-53:5.3.10` base (Dockerfile:1) | Trigger-driven reload via sentinel file, uses `rec_control` binary |

### dnsdist Packet Cache Settings (Primary)

This is the **config of record**. The 2026-08-19 experiment battery kept two changes
(`useClientSubnet=false`, `staleTTL`/`setStaleCacheEntriesTTL` 300 with `keepStaleData=true`);
the full evidence, source-verified rationale, and revert history for every candidate is in
[experiment-log.md](experiment-log.md) (kept: E2, E4; reverted: E1a/E1b/E1c, E3). Rollback
values are carried as inline comments in the template itself.

| Setting | Value | Source Location | Notes |
|---------|-------|-----------------|-------|
| `maxEntries` | 500000 | dnsdist.conf.template:47 | First param to `newPacketCache()` |
| `maxTTL` | 86400 | dnsdist.conf.template:48 | 24 hours max cache time (rollback: 3600) |
| `minTTL` | 1 | dnsdist.conf.template:49 | Minimum 1 second |
| `temporaryFailureTTL` | 5 | dnsdist.conf.template:50 | Cache failures for 5 seconds |
| `staleTTL` | 300 | dnsdist.conf.template:51 | Serve stale for 300 s during outage (rollback: 60) — experiment E4 |
| `dontAge` | false | dnsdist.conf.template:52 | Entries age normally |
| `shuffle` | true | dnsdist.conf.template:53 | Randomize RR order |
| `keepStaleData` | true | dnsdist.conf.template:58 | Required for `staleTTL` to deliver; see E4 below — experiment E4 |
| `setStaleCacheEntriesTTL` | 300 | dnsdist.conf.template:61 | Max age of stale entries served (rollback: 60) — experiment E4 |
| `useClientSubnet` (backend) | false | dnsdist.conf.template:39 | No ECS sent upstream; dead overhead removed — experiment E2 |

**E2/E4 summary** (details and numbers in [experiment-log.md](experiment-log.md)):

- **E2 — `useClientSubnet=false`**: dnsdist appended a /24-truncated ECS option on every
  cache miss that the recursor provably ignored (`use-incoming-edns-subnet=false` default,
  empty `edns-subnet-allow-list`). Hit ratio unchanged at 99.9%, zero lost queries.
  Rollback: set `useClientSubnet=true` and recreate dnsdist.
- **E4 — stale 300/300 + `keepStaleData=true`**: the previous 60 s stale config never
  delivered 60 s — without `keepStaleData=true` the cache cleaner purges expired entries on
  its ~60 s cadence (observed effective window ≤31 s). With the full set, stale serving
  lasts the complete 300 s per entry during a total-backend outage. **Operator-visible
  trade**: during a full upstream outage the edge keeps answering from cache for up to
  300 s per entry (previously effectively ≤31 s); uncached names still fail. Rollback:
  restore `staleTTL=60`, `setStaleCacheEntriesTTL(60)`, remove `keepStaleData=true`,
  recreate dnsdist.

### dnsdist Packet Cache Settings (Secondary Package)

Generated by `node_generator.py` for secondary node packages. Aligned with the
primary's ratified experiment results (E2/E4): `useClientSubnet=false`,
`staleTTL=300` with `keepStaleData=true`, and `setStaleCacheEntriesTTL(300)` -
the earlier pre-E2/E4 lag is closed. Evidence: [experiment-log.md](experiment-log.md).

| Setting | Value | Source Location | Notes |
|---------|-------|-----------------|-------|
| `maxEntries` | 500000 | node_generator.py:300 | Same as primary |
| `maxTTL` | 86400 | node_generator.py:301 | Same as primary |
| `minTTL` | 1 | node_generator.py:302 | Same as primary |
| `temporaryFailureTTL` | 5 | node_generator.py:303 | Same as primary |
| `staleTTL` | 300 | node_generator.py:305 | Same as primary (E4; rollback: 60) |
| `dontAge` | false | node_generator.py:306 | Same as primary |
| `shuffle` | true | node_generator.py:307 | Same as primary |
| `keepStaleData` | true | node_generator.py:311 | Same as primary (E4; required for staleTTL) |
| `setStaleCacheEntriesTTL` | 300 | node_generator.py:315 | Same as primary (E4; rollback: 60) |
| `useClientSubnet` (backend) | false | node_generator.py:296 | Same as primary (E2; rollback: true) |

### Recursor Cache Settings (Primary)

| Setting | Value | Source Location | Notes |
|---------|-------|-----------------|-------|
| `max-cache-entries` | 2000000 | recursor.conf.template:16 | Record cache |
| `max-packetcache-entries` | 1000000 | recursor.conf.template:17 | Packet cache |
| `packetcache-ttl` | 86400 | recursor.conf.template:18 | 24 hours |
| `packetcache-negative-ttl` | 60 | recursor.conf.template:19 | NXDOMAIN/NODATA cache |
| `packetcache-servfail-ttl` | 5 | recursor.conf.template:20 | SERVFAIL cache |

### Recursor Cache Settings (Secondary Package)

Generated by `node_generator.py` for secondary node packages (shipped as
`config/recursor.conf.template` and rendered by the recursor image entrypoint).

| Setting | Value | Source Location | Notes |
|---------|-------|-----------------|-------|
| `max-cache-entries` | 2000000 | node_generator.py:257 | Same as primary |
| `max-packetcache-entries` | 1000000 | node_generator.py:258 | Same as primary |
| `packetcache-ttl` | 86400 | node_generator.py:259 | Same as primary |
| `packetcache-negative-ttl` | 60 | node_generator.py:260 | Same as primary |
| `packetcache-servfail-ttl` | 5 | node_generator.py:261 | Same as primary |

### Cache Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CLIENT                                     │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        dnsdist (:53)                                 │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ Packet Cache: 500k entries, 24h TTL, 300s stale             │    │
│  │ - Hits: instant response                                     │    │
│  │ - Misses: forward to recursor                               │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     recursor (:5300)                                 │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ Packet Cache: 1M entries, 24h TTL                           │    │
│  │ - Caches complete response packets                          │    │
│  └─────────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ Record Cache: 2M entries                                    │    │
│  │ - Caches individual DNS records                            │    │
│  │ - Used for recursive resolution                            │    │
│  └─────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     UPSTREAM DNS                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### Memory Estimates

Based on PowerDNS documentation and empirical data:

| Cache Type | Entry Size (approx) | Total Entries | Memory |
|------------|---------------------|---------------|--------|
| dnsdist packet cache | ~200-500 bytes | 500,000 | ~100-250 MB |
| recursor packet cache | ~200-500 bytes | 1,000,000 | ~200-500 MB |
| recursor record cache | ~300-800 bytes | 2,000,000 | ~600-1600 MB |

**Total estimated cache memory: ~900 MB - 2.5 GB**

---

# DNS Caching Strategy

This document defines the caching strategy for PowerBlockade, including baseline cache settings, image versions, precache policies, domain selection criteria, and primary/secondary synchronization.

## Precache Policy

The precache system proactively warms DNS cache entries for high-frequency (qname, qtype) pairs to reduce latency on first query after cache expiry or service restart.

### Pair Selection Criteria

Warming targets are selected as (qname, qtype) PAIRS, not bare domains, so the edge cache is warmed for the exact question clients asked (A, AAAA, HTTPS/65, ...).

1. **Query Frequency**: Only pairs with successful queries in the last 24 hours are candidates
2. **Success Requirement**: Queries must have returned `rcode=0` (NOERROR)
3. **Non-Blocked Status**: Blocked domains (`blocked=false`) are explicitly excluded
4. **Ranking**: Pairs are ranked by per-pair query count, with the highest-frequency pairs prioritized
5. **Request Ceiling**: Each warming pass sends at most `precache_max_queries_per_pass` queries (default 2000)

**Answer-present proxy**: `dns_query_events` has no answer-count column, so NOERROR-with-no-answers (NODATA) pairs cannot be distinguished from pairs that returned records. `rcode=0` is the best available proxy for "warming-positive"; NODATA pairs are still worth warming because their negative answers are cached too. If an answer-count column is added later, selection should prefer answer-bearing pairs.

**Selection SQL Logic** (from `admin-ui/app/services/precache.py`):

```sql
SELECT qname, qtype, COUNT(id) as count
FROM dns_query_events
WHERE ts >= (NOW() - INTERVAL '24 hours')
  AND blocked = FALSE
  AND rcode = 0
GROUP BY qname, qtype
ORDER BY count DESC
LIMIT :domain_count
```

### Warming and the Two Cache Layers

Warming queries are sent THROUGH the dnsdist edge (`precache_dns_server` / `precache_dns_port`, default `dnsdist:53`), so each warmed pair refreshes the **edge layer**: the dnsdist packet cache holds an entry for that exact (qname, qtype) until its TTL expires.

A query that HITS the dnsdist packet cache never reaches the recursor. An ordinary warm pass therefore cannot by itself guarantee the inner recursor record/packet caches stay populated — that is expected and by design:

* Warm queries that MISS the edge cache are recursed by dnsdist, populating the recursor caches along the way.
* The recursor's own caches plus its near-expiry refresh keep the inner layer warm between passes.
* The recursor-side refresh experiment (E3, `refresh-on-ttl-perc=10`) ran and was
  **reverted**: edge and inner entries expire in lockstep, so refresh tasks effectively
  never fire behind a ~99.9% edge cache (taskqueue-pushed delta 0 over a 240s steady-load
  window). Evidence: [experiment-log.md](experiment-log.md) E3.

TTL bookkeeping is per (qname, qtype) pair: each pair's refresh cadence respects the shortest TTL observed for that pair (minimum TTL across the answer RRsets of its most recent successful response; NODATA responses use the SOA negative TTL). The same qname can legitimately be warmed on different cadences for different qtypes — e.g. `example.com A` at TTL 300 refreshes at 240s while `example.com AAAA` at TTL 60 refreshes at 30s.

### Boot Warm Burst

On admin-ui startup, a one-shot **boot warm burst** re-warms the top observed pairs as soon
as the DNS edge is actually serving, so a restarted stack does not wait for the first
scheduled pass (`precache_refresh_minutes`, default 30). Implementation:
`admin-ui/app/services/boot_burst.py`.

| Property | Value | Source |
|---|---|---|
| Trigger | admin-ui startup, daemon thread | boot_burst.py (`start_boot_burst`, main.py:170) |
| Readiness gate | polls dnsdist (:53) and recursor (:5300) health before sending anything | boot_burst.py `wait_for_dns_readiness` |
| Locking | shares the `precache_warming` advisory lock with the scheduled job — never double-fires | boot_burst.py `WARMING_LOCK_JOB_NAME` |
| Rate bound | hard QPS ceiling, jitter only ever ADDS delay (never exceeded) | boot_burst.py `QPSPacer`, `JITTER_RATIO=0.10` |
| Per-burst cap | `precache_max_queries_per_pass` (same ceiling as a normal pass) | settings.py |
| Result | structured summary at `GET /precache/boot-burst` and on the precache page | routers/precache.py |
| Settings | `precache_boot_burst_enabled` (default true), `precache_boot_burst_concurrency` (default 8), `precache_boot_burst_qps` (default 50) | settings.py:47-49 |

Disable by setting `precache_boot_burst_enabled=false` (admin UI settings form or the
settings table); the scheduled warming job is unaffected.

### Domain Exclusion Criteria

The following domain categories should **never** be precached due to volatility or security concerns:

| Category | Rationale | Examples |
|----------|-----------|----------|
| **CDN/Edge Domains** | IP addresses change frequently based on geography and load | `*.cloudfront.net`, `*.cdn.cloudflare.com`, `*.akamaized.net` |
| **Security-Sensitive Domains** | Stale responses could create security risks | Banking, authentication, certificate revocation |
| **Dynamic DNS Services** | TTL typically very short, IPs change frequently | `*.dyndns.org`, `*.no-ip.com`, `*.ddns.net` |
| **Short-TTL Domains** | Domains with TTL < 60 seconds should be refreshed at query time | Some API endpoints, load balancer VIPs |
| **Blocked Domains** | Already filtered out by query logic | All domains matching RPZ blocklists |

**Recommended Exclusion Patterns** (for future implementation):

```
# CDN patterns
*.cloudfront.net
*.akamaized.net
*.fastly.net
*.cdn.cloudflare.com

# Dynamic DNS
*.dyndns.*
*.no-ip.*
*.ddns.*

# Security-sensitive
*.bank.*
*.paypal.*
login.*
auth.*
sso.*
```

### Refresh Cadence Policy

The precache system supports two refresh modes:

#### TTL-Aware Refresh (Default: `precache_ignore_ttl=false`)

- **Behavior**: Respects upstream TTL with a 20% safety margin, per (qname, qtype) pair
- **Formula**: `refresh_threshold = ttl - max(ttl * 0.2, 30 seconds)` where `ttl` is the shortest TTL observed for the pair
- **Example**: A pair with 300s TTL refreshes at 240s (300 - 60); the same qname with a 60s TTL on another qtype refreshes at 30s
- **Minimum Margin**: 30 seconds (prevents rapid refresh of short-TTL pairs)

#### Fixed Interval Refresh (Override: `precache_ignore_ttl=true`)

- **Behavior**: Ignores upstream TTL, refreshes on fixed schedule
- **Default**: 60 minutes (`precache_custom_refresh_minutes`)
- **Use Case**: Environments with unstable upstream DNS where TTL is unreliable

#### Primary Node Schedule

| Setting | Default | Description |
|---------|---------|-------------|
| `precache_refresh_minutes` | 30 | How often the precache job runs (checks which domains need warming) |
| `precache_custom_refresh_minutes` | 60 | Fixed refresh interval when TTL is ignored |

### TTL-Respect Policy

| Policy | Setting | Behavior |
|--------|---------|----------|
| **Honor TTL** | `precache_ignore_ttl=false` (default) | Domains refreshed before TTL expires with 20% safety margin |
| **Ignore TTL** | `precache_ignore_ttl=true` | All domains refreshed on fixed interval regardless of upstream TTL |

**Recommendation**: Use TTL-aware refresh (default) for most deployments. Only use fixed interval if upstream DNS servers provide unreliable TTL values.

### Primary vs Secondary Sync Timing

#### Secondary Node Precache Behavior

Secondary nodes run their own precache warming independently but use the **primary node's domain list** as the source of truth.

**Sync Flow**:

1. Primary node computes top domains from its query log (24h window, top N domains)
2. Secondary fetches domain list via `/api/node-sync/precache-domains`
3. Secondary resolves each domain against its **local recursor** (not primary)
4. Secondary warms its local cache with resolved entries

**Timing Configuration** (sync-agent):

| Setting | Default | Description |
|---------|---------|-------------|
| `PRECACHE_INTERVAL_SECONDS` | 300 | Seconds between secondary precache runs |
| `CONFIG_SYNC_INTERVAL_SECONDS` | 300 | Seconds between config sync checks |

#### Expected Drift Windows

| Scenario | Drift | Impact |
|----------|-------|--------|
| Normal operation | 0-5 minutes | Negligible - both nodes warm similar domains |
| Primary outage | Up to `PRECACHE_INTERVAL_SECONDS` | Secondary continues using last-known-good domain list |
| New high-traffic domain | Up to 24 hours | Domain appears in next day's top domains list |

**Drift Tolerance**: Secondaries may have slightly different cache contents than primary, but all nodes resolve from authoritative upstream, so results remain consistent.

### Risk Assessment

#### Stale Cache Risks

| Risk | Mitigation |
|------|------------|
| IP address changes | TTL-aware refresh with safety margin |
| CDN routing changes | Exclude CDN domains from precache |
| DNS failover events | Short TTL domains refresh more frequently |

#### Premature Refresh Risks

| Risk | Mitigation |
|------|------------|
| Increased upstream queries | Safety margin prevents excessive refresh |
| Cache thrashing | Minimum 30s margin on short-TTL domains |
| Resource exhaustion | Batch processing with 100ms delay between batches |

### Configurable Knobs

| Setting | Default | Range | Description |
|---------|---------|-------|-------------|
| `precache_enabled` | true | boolean | Enable/disable precache warming |
| `precache_domain_count` | 5000 | 100-10000 | Number of top pairs to track |
| `precache_refresh_minutes` | 30 | 5-1440 | Job run frequency |
| `precache_ignore_ttl` | false | boolean | Ignore upstream TTL |
| `precache_custom_refresh_minutes` | 60 | 10-1440 | Fixed refresh interval when TTL ignored |
| `precache_dns_server` | dnsdist | hostname | DNS server (edge) to query for warming |
| `precache_dns_port` | 53 | 1-65535 | Port for DNS queries (dnsdist edge) |
| `precache_max_queries_per_pass` | 2000 | 100-100000 | Ceiling on warm queries per pass |
| `precache_boot_burst_enabled` | true | boolean | Run the readiness-gated boot warm burst on admin-ui startup |
| `precache_boot_burst_concurrency` | 8 | 1-64 | Boot burst worker concurrency (clamped) |
| `precache_boot_burst_qps` | 50 | 1-1000 | Boot burst hard QPS ceiling (clamped) |

**Settings migration note**: data migration `0019_precache_warming_dnsdist`
(admin-ui/alembic/versions/) flips stored rows still holding the pre-P7 defaults
(`precache_dns_server` 'recursor' → 'dnsdist', `precache_dns_port` '5300' → '53') so
deployments that once saved the precache form warm through the edge after upgrading.
Explicit user overrides are left untouched, and the migration is idempotent.

### Implementation Details

**Primary Node** (`admin-ui/app/services/precache.py`):
- `get_top_pairs_to_warm()`: Queries last 24h of successful, non-blocked queries grouped by (qname, qtype)
- `get_pairs_needing_refresh()`: Filters pairs that are stale based on per-pair TTL/interval
- `warm_cache()` / `warm_pair()`: Re-ask each observed pair with its own qtype through the dnsdist edge (raw `dns.message.make_query` + `dns.query.udp`, since `Resolver.resolve()` cannot ask arbitrary qtypes), in batches of 50 with 100ms inter-batch delay, capped by `precache_max_queries_per_pass`

**Secondary Node** (`sync-agent/agent.py`):
- `fetch_precache_domains()`: Retrieves domain list from primary
- `run_precache_warming()`: Resolves domains against local recursor
- Runs every `PRECACHE_INTERVAL_SECONDS` (default: 5 minutes)

### Monitoring and Observability

**Metrics Available**:
- `cached_pairs`: Total (qname, qtype) pairs in the TTL cache
- `fresh`: Pairs still within TTL
- `expired`: Pairs past TTL needing refresh
- `by_qtype`: Pair counts per qtype

**Log Indicators**:
- `Cache warming: N/M pairs in Xms` - Successful warming pass
- `All N pairs still fresh, skipping` - No refresh needed
- `Failed to warm qname/qtype: error` - Individual query failure

### Best Practices

1. **Start conservative**: Begin with 1000-2000 domains, increase based on memory
2. **Monitor cache hit rate**: If hit rate drops, may need more domains
3. **Exclude volatile domains**: Implement exclusion patterns for CDNs and dynamic DNS
4. **Tune refresh interval**: Balance between freshness and upstream query load
5. **Secondary autonomy**: Each secondary warms independently for resilience

---

# Tuning Matrix

This section provides a setting-by-setting matrix for cache and precache tuning, including safe ranges, expected effects, failure modes, and rollback actions.

## One-Layer-at-a-Time Guidance

**CRITICAL**: Never tune dnsdist AND recursor cache settings simultaneously. Each layer must be validated independently before moving to the next layer.

**Recommended tuning sequence**:
1. **Phase 1**: dnsdist packet cache only
2. **Phase 2**: Recursor cache only (after dnsdist validated)
3. **Phase 3**: Precache scheduler knobs only (after both cache layers validated)

**Why this matters**: Tuning multiple layers simultaneously makes it impossible to attribute performance changes or issues to a specific setting. If something breaks, you won't know which layer caused it.

---

## dnsdist Packet Cache Tuning Matrix

These settings control the first-line packet cache at the dnsdist layer. Queries that hit this cache never reach the recursor.

| Setting | Current Value | Safe Range | Expected Effect | Failure Mode | Rollback Action |
|---------|---------------|------------|-----------------|--------------|-----------------|
| `maxEntries` | 500000 | 100000-1000000 | More entries = higher hit rate for diverse query sets | OOM if set too high for available RAM; pre-allocation wastes memory if set too high | Reduce by 50% and restart dnsdist container |
| `maxTTL` | 86400 (24h) | 3600-172800 | Longer TTL = more cache hits but potentially stale data | Stale DNS responses if upstream IPs change; blocked domain updates delayed | Reduce to 3600 and flush cache with `getPool(''):getCache():expunge(0)` |
| `minTTL` | 1 | 0-60 | Filters out very short-TTL entries from cache | Entries with TTL < minTTL bypass cache, increasing upstream load | Set to 0 or 1 |
| `temporaryFailureTTL` | 5 | 0-60 | Caches SERVFAIL/REFUSED responses briefly | 0 = no caching of failures = more upstream load; too high = clients see cached failures | Reduce to 0 to disable failure caching |
| `staleTTL` | 300 | 0-300 | TTL used when serving stale entries during backend outage | 0 = no stale serving = errors during outages; too high = very stale data | Reduce to 60 (pre-E4 value) and restart dnsdist |
| `dontAge` | false | true/false | true = entries keep original TTL; false = TTL counts down | true = stale data appears fresh to clients; false = normal behavior | Set to false |
| `shuffle` | true | true/false | true = randomize RR order (load distribution); false = preserve order | May affect clients expecting consistent ordering | Set to false |
| `keepStaleData` | true | true/false | Pauses the cache cleaner's purge of expired entries while ALL backends of a pool are down | false = cleaner purges expired entries on its ~60s cadence, silently defeating `staleTTL` (observed: 60s nominal → ≤31s effective) | Remove `keepStaleData=true` (pre-E4 value) |
| `setStaleCacheEntriesTTL()` | 300 | 0-300 | Max age of expired entries eligible for stale serving | 0 = stale disabled; too high = very old data served | Reduce to 60 (pre-E4 value) |

### dnsdist Tuning Notes

- **Memory impact**: Each entry pre-allocates ~12-20 bytes plus response size. 500k entries with 512-byte avg = ~250 MB.
- **Cache collision warning**: Very large caches may have more hash collisions. Monitor via `getPool(''):getCache():printStats()`.
- **Stale serving interaction**: `staleTTL`, `setStaleCacheEntriesTTL()`, and `keepStaleData` must move together — the E4 finding is that raising the stale TTLs without `keepStaleData=true` is cosmetic (the cleaner purges before the window can be used). Evidence: [experiment-log.md](experiment-log.md) E4.
- **Backend ECS**: `useClientSubnet=false` is the config of record; the recursor ignores incoming ECS by default, so re-enabling it only adds per-miss overhead. Evidence: [experiment-log.md](experiment-log.md) E2.

---

## Recursor Cache Tuning Matrix

These settings control the recursor's record and packet caches. The recursor handles recursive resolution and has two cache layers.

### Record Cache Settings

| Setting | Current Value | Safe Range | Expected Effect | Failure Mode | Rollback Action |
|---------|---------------|------------|-----------------|--------------|-----------------|
| `max-cache-entries` | 2000000 | 500000-5000000 | More entries = more cached records for resolution | OOM; excessive memory pressure on container | Reduce by 50% and restart recursor |
| `max-cache-ttl` | (default) | 86400-604800 | Max time records stay in cache | Stale NS/A records cause resolution failures | Not currently set; add with conservative value |
| `max-negative-ttl` | (default) | 60-3600 | Max time NXDOMAIN/NODATA cached | Too high = blocked domains appear blocked after unblock; too low = extra upstream queries | Not currently set; add with 300s value |

### Packet Cache Settings

| Setting | Current Value | Safe Range | Expected Effect | Failure Mode | Rollback Action |
|---------|---------------|------------|-----------------|--------------|-----------------|
| `max-packetcache-entries` | 1000000 | 100000-2000000 | More entries = more cached response packets | OOM; memory pressure | Reduce by 50% and restart |
| `packetcache-ttl` | 86400 | 3600-172800 | Max TTL for cached response packets | Stale responses; delayed blocklist updates | Reduce to 3600, flush with `rec_control wipe-cache '$'` |
| `packetcache-negative-ttl` | 60 | 0-300 | TTL for NXDOMAIN/NODATA responses | 0 = no negative caching; too high = stale NXDOMAIN | Reduce to 30 or 0 |
| `packetcache-servfail-ttl` | 5 | 0-60 | TTL for SERVFAIL responses | 0 = no caching; too high = clients see cached failures | Reduce to 0 |
| `disable-packetcache` | no | yes/no | Disables packet cache entirely | yes = all queries hit record cache = slower | Ensure set to 'no' |
| `packetcache-shards` | (default) | 1-1024 | Number of hash shards for packet cache | Too few = lock contention; too many = memory overhead | Not currently set; tune only if needed |

### Recursor Tuning Notes

- **Two cache types**: Record cache stores individual DNS records; packet cache stores complete response packets. Both matter.
- **Memory math**: 2M record entries @ 500 bytes avg = ~1 GB. 1M packet entries @ 300 bytes avg = ~300 MB.
- **Negative cache interaction**: `packetcache-negative-ttl` affects how quickly unblocks propagate. Keep low (60s max).
- **Flush command**: `rec_control wipe-cache '$'` clears all caches without restart.

---

## Precache Scheduler Tuning Matrix

These knobs control the proactive cache warming system that pre-populates cache entries for high-frequency domains.

| Setting | Current Value | Safe Range | Expected Effect | Failure Mode | Rollback Action |
|---------|---------------|------------|-----------------|--------------|-----------------|
| `precache_enabled` | true | true/false | Enables/disables precache warming | false = cold cache after restart = slow first queries | Set to true |
| `precache_domain_count` | 5000 | 100-10000 | Number of top (qname, qtype) pairs to track | Too high = excessive upstream queries during warming; too low = cache miss for popular pairs | Reduce to 1000-2000 |
| `precache_refresh_minutes` | 30 | 5-1440 | How often precache job runs | Too frequent = wasted CPU; too rare = stale entries | Increase to 60 if upstream load is concern |
| `precache_ignore_ttl` | false | true/false | true = ignore upstream TTL; use fixed interval | true = ignores pair TTL signals = potentially stale data; false = respects TTL | Set to false |
| `precache_custom_refresh_minutes` | 60 | 10-1440 | Fixed refresh interval when TTL ignored | Only used if `precache_ignore_ttl=true` | Not applicable if TTL respected |
| `precache_dns_server` | dnsdist | hostname/IP | Edge DNS server warming goes through | Wrong server = no edge cache warming benefit | Set to 'dnsdist' or correct IP |
| `precache_dns_port` | 53 | 1-65535 | Port for precache DNS queries (dnsdist edge) | Wrong port = warming fails silently | Verify matches dnsdist listener port |
| `precache_max_queries_per_pass` | 2000 | 100-100000 | Ceiling on warm queries per pass | Too low = pairs expire between passes; too high = burst load on edge | Keep near default; raise only with observed miss rate |

### Secondary Node Precache Knobs

| Setting | Current Value | Safe Range | Expected Effect | Failure Mode | Rollback Action |
|---------|---------------|------------|-----------------|--------------|-----------------|
| `PRECACHE_INTERVAL_SECONDS` | 300 | 60-3600 | Seconds between secondary precache runs | Too low = excessive CPU/network; too high = stale cache | Increase to 600 if concerned |
| `CONFIG_SYNC_INTERVAL_SECONDS` | 300 | 60-1800 | Seconds between config sync checks | Too low = API load; too high = delayed config propagation | Adjust based on change frequency |

### Precache Tuning Notes

- **TTL safety margin**: System uses `max(ttl * 0.2, 30)` as refresh margin, per (qname, qtype) pair. Short-TTL pairs (< 150s) always use 30s margin.
- **Batch processing**: Pairs warmed in batches of 50 with 100ms delay between batches, capped per pass by `precache_max_queries_per_pass`.
- **Secondary autonomy**: Secondaries fetch domain LIST from primary but resolve locally. Primary outage = secondaries use stale list.
- **Exclusion gap**: Current implementation lacks CDN/dynamic DNS exclusion patterns. This should be addressed.

---

## Interaction Warnings

### Cache Size vs Memory Usage

| Interaction | Warning |
|-------------|---------|
| dnsdist `maxEntries` + recursor caches | Total memory = dnsdist packet cache + recursor packet cache + recursor record cache. Monitor container memory before increasing any cache size. |
| `max-cache-entries` + `max-packetcache-entries` | Both contribute to recursor memory. Doubling both = ~2x recursor memory. |
| High `precache_domain_count` + large caches | Precache warming fills caches. If cache sizes are small relative to domain count, warming provides limited benefit. |

### TTL Settings vs Stale Data

| Interaction | Warning |
|-------------|---------|
| `maxTTL` + blocklist updates | Blocklist changes may not take effect until cache entries expire. High maxTTL = delayed blocking. |
| `packetcache-negative-ttl` + unblocking | Domains removed from blocklist may still return NXDOMAIN until negative cache expires. Keep this value low. |
| `dontAge=true` + any TTL | Entries never appear to age from client perspective. Can mask stale data. Avoid unless specifically needed. |
| `precache_ignore_ttl=true` + short-TTL domains | Short-TTL domains will be cached longer than intended by domain owner. May cause issues with CDNs/load balancers. |

### Negative Cache vs Blocking Propagation

| Interaction | Warning |
|-------------|---------|
| `packetcache-negative-ttl` + RPZ blocking | Blocked domains return NXDOMAIN. If negative TTL is high and domain is unblocked, NXDOMAIN persists. |
| dnsdist `temporaryFailureTTL` + upstream issues | Cached SERVFAIL responses mask transient upstream issues. Clients may see failures even after upstream recovers. |
| `max-negative-ttl` + new domains | Newly created domains may be cached as NXDOMAIN if queried before delegation. |

### Stale Serving Interactions

| Interaction | Warning |
|-------------|---------|
| `staleTTL` + `setStaleCacheEntriesTTL()` + `keepStaleData` | All three must move together: without `keepStaleData=true` the cache cleaner purges expired entries on its ~60s cadence and the stale window collapses (E4 finding: 60s nominal delivered ≤31s effective). If either TTL is 0, stale serving is effectively disabled. |
| Stale serving + blocklist changes | Stale responses may include records for domains that were just blocked. Acceptable tradeoff for availability — wider with E4 (up to 300s per entry). |
| Stale serving + DNSSEC | Stale responses may have expired RRSIG records. DNSSEC-validating clients may reject them. |

---

## Tuning Change Procedure

### Before Any Change

1. **Record baseline**: Note current cache hit rate, latency, memory usage
2. **Check available memory**: Ensure container has headroom for cache growth
3. **Plan rollback**: Know exact command to revert change

### dnsdist Changes

```bash
# 1. Edit config
vim dnsdist/dnsdist.conf.template

# 2. Validate syntax (dnsdist --check-config)
docker compose exec dnsdist dnsdist --check-config -C /etc/dnsdist/dnsdist.conf

# 3. Restart dnsdist only
docker compose restart dnsdist

# 4. Verify cache is working
docker compose exec dnsdist dnsdist -e "getPool(''):getCache():printStats()"

# Rollback if needed
git checkout -- dnsdist/dnsdist.conf.template
docker compose restart dnsdist
```

### Recursor Changes

```bash
# 1. Edit config
vim recursor/recursor.conf.template

# 2. Validate config
# (Recursor validates on startup; check logs for errors)

# 3. Restart recursor only
docker compose restart recursor

# 4. Verify cache is working
docker compose exec recursor rec_control get all-outqueries

# Alternative: Flush cache without restart
# docker compose exec recursor rec_control wipe-cache '$'

# Rollback if needed
git checkout -- recursor/recursor.conf.template
docker compose restart recursor
```

### Precache Changes

```bash
# 1. Update via admin UI Settings page
# OR update directly in database

# 2. Changes take effect on next scheduler run
# Or trigger manually via /jobs endpoint

# Rollback: Revert setting value in admin UI
```

---

## Version Compatibility Notes

### dnsdist Version 20 Series

- Uses Lua-style `newPacketCache(size, {options})` syntax
- `setStaleCacheEntriesTTL()` is a global function, not cache option
- YAML configuration also supported but not used in PowerBlockade

### Recursor Version 53 Series

- Old-style settings require `--enable-old-settings` flag on 5.2+
- YAML settings are the future; migration path exists
- `rec_control wipe-cache` works on all versions

---

## Monitoring Tuning Impact

### Key Metrics to Watch

| Metric | Source | Good Range | Warning Sign |
|--------|--------|------------|--------------|
| dnsdist cache hit ratio | `getPool(''):getCache():printStats()` | > 80% | < 60% after tuning |
| Recursor cache hit ratio | `rec_control get cache-hits/cache-misses` | > 70% | < 50% after tuning |
| Memory usage | Docker stats / Prometheus | < 80% of limit | > 90% of limit |
| Query latency p95 | Prometheus metrics | < 10ms | > 50ms |
| Upstream queries | Recursor `all-outqueries` | Stable or decreasing | Increasing after cache tuning |

### Observation Period

- **Immediate**: Restart success, no errors in logs
- **5 minutes**: Cache hit rates stabilizing
- **1 hour**: Latency distribution visible
- **24 hours**: Memory usage pattern clear
- **1 week**: Seasonal traffic patterns visible

---

## Quick Reference: Rollback Commands

| Layer | Scenario | Command |
|-------|----------|---------|
| dnsdist | Any cache issue | `docker compose restart dnsdist` |
| dnsdist | Stale data | `docker compose exec dnsdist dnsdist -e "getPool(''):getCache():expunge(0)"` |
| dnsdist | Revert E2 (ECS off) | Set `useClientSubnet=true` in `dnsdist/dnsdist.conf.template` and recreate dnsdist |
| dnsdist | Revert E4 (stale 300 + keepStaleData) | Restore `staleTTL=60`, `setStaleCacheEntriesTTL(60)`, remove `keepStaleData=true` and recreate dnsdist |
| recursor | Any cache issue | `docker compose restart recursor` |
| recursor | Stale data | `docker compose exec recursor rec_control wipe-cache '$'` |
| recursor | Config error | `git checkout -- recursor/recursor.conf.template && docker compose restart recursor` |
| Both | Full reset | `docker compose restart dnsdist recursor` |
| precache | Disable warming | Set `precache_enabled=false` in admin UI |
| precache | Disable boot burst only | Set `precache_boot_burst_enabled=false` in admin UI |
